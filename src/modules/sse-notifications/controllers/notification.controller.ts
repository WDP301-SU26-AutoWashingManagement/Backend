// Controller logic is handled inline in the SSE route handler
// because SSE connections are long-lived and don't follow
// the standard request → response → done pattern.
//
// See: ../routes/notification.route.ts
